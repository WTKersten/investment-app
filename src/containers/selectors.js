export const selectProfileName = state => state.investments.profile.name;
export const selectStocks = state => state.investments.stocks;
export const selectNews = state => state.investments.news;
export const selectGenericLoadingCalls = state => state.investments.genericLoadingCalls;